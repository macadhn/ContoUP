on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build with Buildozer
        uses: NiklasSjostrand/buildozer-action@v1.2
        with:
          command: android debug
